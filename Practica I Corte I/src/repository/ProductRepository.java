/*ackage repository;

import domain.enums.ProductType;
import domain.models.Product;

import java.util.List;

public interface ProductRepository {
    Product createClient(Product product);
    Product updateClient(Product product);
    String removeClient(String productId);

    List<Product> listAllClients();

    List<Product> listAllByType(ProductType productType);
}*/